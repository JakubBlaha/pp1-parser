"""Worked examples from the formalism, expressed with the ``verifier`` DSL.

Each module (``00.py`` .. ``13.py``) builds one ``Requirement`` from
``resources/formalism/tex-new/examples.tex`` and can be run directly to print
its canonical JSON, e.g. ``python examples/07.py``.
"""
